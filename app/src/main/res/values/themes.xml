<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TheSportsApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>



    </style>

    <style name="Theme.TheSportsApp.Splash" parent="Theme.SplashScreen">
        <!-- Fundo -->
        <item name="windowSplashScreenBackground">@color/splash_background</item>

        <!-- Ícone -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/sports_icon</item>

        <!-- Cor do ícone -->
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_icon_bg</item>

        <!-- Tema após a splash -->
        <item name="postSplashScreenTheme">@style/Theme.TheSportsApp</item>
    </style>

</resources>